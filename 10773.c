#include <stdio.h>
#include <stdlib.h>
int main(void){
   printf("hello world!");
   /*
    int a,b;
    scanf("%d",&a);
    int *p = (int*)malloc(sizeof(int) * a);
    for(int i = 0; i < a; i++){
        scanf("%d",&p[i]);
    }
    for(int i = 0; i < a; i++){
        if(p[i] == 0){
            continue;
        }
        else if(p[i] + p[i+1] == p[i]){
            p[i] = 0;
        }
    }
    for(int i = 0; i < a; i++){
        b = b + p[i];
    }
    printf("%d",b);
    */
}
